#pragma once
#include <vector>

class MoveStack {
// private:
//     int moveIdx;
//     std::vector<Move> moves;
// public:
//     Move& next();
//     Move& previous();

//     bool hasNext();
//     bool hasPrevious();

//     void addMove(Move& move);
};